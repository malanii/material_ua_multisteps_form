import React from "react";
import Container from '@material-ui/core/Container';

export const Submit = () =>{
    return (
        <Container maxWidth="md" style={{marginTop: "2rem", textAlign: 'center'}}>
            <h2>Thank you for Submitting! We will stay in touch!</h2>
        </Container>
    )
};